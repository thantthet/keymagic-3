export struct KeyboardInfo {
    id: string,
    name: string,
    description: string,
    hotkey: string,
    default-hotkey: string,
    custom-hotkey: string,
    active: bool,
    enabled: bool,
    has-icon: bool,
    icon: image,
    // Parsed hotkey components
    hotkey-ctrl: bool,
    hotkey-alt: bool,
    hotkey-shift: bool,
    hotkey-key: string,
}